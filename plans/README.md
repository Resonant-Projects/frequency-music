# Implementation Plans

Generated by the improve skill on 2026-06-12, against commit `e07be2e`. The session ran
non-interactively, so per the skill's default the **top 5 findings by leverage** were planned;
everything else found by the audit is recorded below so it can be planned on request without
re-auditing. Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update your row when done.

Audit scope: full repo at `standard` depth (all nine categories; correctness + security swept
"very thorough", the rest "medium"). **Not audited**: `docs/essays/` prose, `data/` manifests,
Panda CSS styling layers, the deployed Convex instance's runtime config (only repo evidence),
and `agent/` beyond its src/tests surface.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Remove committed auth-bypass secret (10 locations) + rotation checklist | P1 | S | — | DONE — implemented via varlock + 1Password (2026-06-14); secret removed from all 10 sites, scripts load `varlock/auto-load`, `.env.schema` added (root/web/agent). Pending operator: create vault + rotate. |
| 002 | Convert `convex/graph.ts` writes to internal functions | P1 | S | — | TODO |
| 003 | CI on existing verification commands + backend typecheck burn-down (107 errors) | P1 | M | — | TODO |
| 004 | Fix weekly-brief generation (`ctx.db` in actions crashes every run) | P1 | M | — (best after 003) | TODO |
| 005 | Per-source transactional extraction dedupe + stuck-`extracting` recovery | P2 | M | — (best after 003) | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- 001 and 002 are independent of everything — land first; they are the security exposure.
- 003 is the verification baseline: once its typecheck gate exists, plans 004/005 (and all future work)
  get compile-time protection for the exact bug classes they fix. 004/005 do not *require* 003, but
  their scoped-typecheck done-criteria simplify to `bun run typecheck` once 003 lands.
- 003 deliberately does NOT touch `weeklyBriefs.ts`'s `ctx: any` (004 owns it) or `graph.ts`
  registration (002 owns it). If executed out of order, re-run 003's error inventory.
- 001's rotation step interacts with TESTS-04 (e2e suite reads the same secret): after rotation,
  e2e needs `AUTH_BYPASS_SECRET` in env or it fails loudly (by design after 001).

## Vetted findings not yet planned (backlog, ordered by leverage)

Every row was verified against the code by the advisor (file:line opened and read). Ask for a plan
for any of these by ID.

| ID | Finding | Category | Impact | Effort | Risk | Evidence (verified) |
|----|---------|----------|--------|--------|------|---------------------|
| SECURITY-03 | Public read queries expose full `rawText`/`transcript`, ignoring the visibility tier system; app is supposed to be auth-gated (decision log 2026-04-01) | security | Full article text + Notion notes readable by any anonymous caller | M | MED | `convex/sources.ts:29-93` (listByStatus/listRecent/listByType/get, no auth), `convex/validators.ts:81-83` (rawText/transcript in return validator), `extractions.ts`/`dashboard.ts`/`failures.ts` have zero `requireAuth`; contrast sanitized `agentRuns.ts:77-103` |
| SECURITY-04 | Secrets transit plaintext HTTP: CI sends `AUTH_BYPASS_SECRET` to the http:// backend default; agent tools POST `AGENT_TOOL_SECRET` to http:// | security | Write credentials interceptable on-path; nullifies 001's rotation if unfixed | M (infra: TLS on PaaS ingress; code: find-replace) | LOW | `.github/workflows/public-editorial-export.yml:29-30`, `agent/.env.example:2`, `scripts/smart-fetch.ts:18-21`, CLAUDE.md/AGENTS.md:162 |
| CORRECTNESS-06 | Three divergent dedupe-key schemes: cron RSS `rss:{hostname}:{guid}` vs webhook `rss:{feedUrl}:{guid}`; `ingestUrl` drops query strings (collapses `?id=1`/`?id=2`); two drifted YouTube ID extractors | bug | Duplicate sources (double LLM spend) + silently lost query-string URLs | M | MED (needs key migration/merge of existing rows) | `convex/ingest.ts:120-123` vs `convex/http.ts:228-233`+`sourceUtils.ts`; `ingest.ts:303`; `ingest.ts:357-368` vs `sourceUtils.ts:39-51` |
| CORRECTNESS-08 / SECURITY-07 | `extract.ts` trusts LLM/scraped content: greedy `/\{[\s\S]*\}/` + bare `JSON.parse`, missing-array crashes, `as any` enum passthrough, and `String.replace` `$`-pattern expansion corrupting the prompt template | bug/security-hardening | Model hiccups burn paid generations into `review_needed`; scraped `$&` sequences corrupt prompts | M | LOW | `convex/extract.ts:203-205,234-239,260,283-292`; contrast defensive `recipes.ts:69-184`, `weeklyBriefs.ts:230-276` |
| SECURITY-08 | No `convex/auth.config.ts` anywhere — Clerk JWTs may never validate server-side; if so, ALL auth reduces to the bypass secret (web/src never passes `devBypassSecret` — verified) | security | Possibly no working identity path in prod | S (investigate first) | LOW | Tree-wide search: no auth.config.*; `convex/auth.ts:52`; web grep: zero devBypassSecret | **Investigate-grade**: deployed state unverifiable from repo |
| TESTS-03 | Three divergent hand-rolled Convex fakes; shared fake ignores index names, always sorts by `updatedAt` (real Convex sorts by index key, e.g. `by_weekOf`), `makeDb` is read-only so mutations are untestable | tests | Tests can pass against fakes and fail against real Convex; mutation coverage impossible | M | MED | `convex/testHelpers.ts:18-61`, own fakes in `failures.test.ts:28` + `dashboard.test.ts:11` | Fix: adopt official `convex-test`, port 39 tests |
| TESTS-02 | Zero coverage on core pipeline modules (extract 411L, ingest 441L, sources 648L, recipes 624L, graph 984L); high churn + no tests | tests | Regressions corrupt research data; no staging env | L (first slice M) | LOW | Test-file listing vs module listing; 3-mo churn: compositions 10, hypotheses 8, graph 7, extract 6 | Blocked-ish on TESTS-03 for mutation tests |
| CORRECTNESS-05 | `hypotheses.generateBatch` has no "already hypothesized" check — every run re-derives from the same top extractions | bug | Duplicate hypothesis rows + wasted LLM spend per batch run | S | LOW | `convex/hypotheses.ts:505-521` (verified: filters only claim/param counts); contrast `recipes.ts:579-587` |
| CORRECTNESS-07 | `lastItemAt` erased on every quiet poll (`patch` with `undefined` unsets in Convex) | bug | Dead-feed detection impossible from data (live problem: 6 dead feeds) | S | LOW | `convex/ingest.ts:141,168-182,214-219` → `convex/feeds.ts:86-92` |
| PERF-01 / DEBT-01 | `dashboard:pipeline` collects six entire tables for counts on the home route; installed `@convex-dev/aggregate` is never maintained (writes only in manual rebuilds, no cron) and never consumed | perf | Home-page reactive query re-reads everything on any write; grows linearly | M | MED | `convex/dashboard.ts:64-83`, `convex/aggregates.ts:136-167`, `convex/crons.ts` (23 lines, no rebuild), `web/src/integrations/convex/api.ts` (zero aggregates refs) | Wire Triggers (convex-helpers already a dep) before consuming counts |
| PERF-02 | `failures:listArchive` re-derives the whole archive (full collects + per-composition BFS) per query run; `limit` applied post-derivation | perf | Steepest-growing query; backs a routed page + plan 004's brief context | M | MED | `convex/failures.ts:254,279,318,486` | Derived-view *design* is settled (decision log) — only the recomputation is the finding |
| PERF-03 | `computeEditorialSignals` O(concepts × hypotheses) nested scan with per-pair lowercasing; Maps built for 3 other joins but not this one | perf | Quadratic CPU in a reactive query over 5 tables | S | LOW | `convex/dashboard.ts:301-345` (verified Maps at :316-335, nested filter at :337-342) |
| TESTS-04 | E2E suite needs undocumented env, mutates the live shared backend, and falls back to triaging an arbitrary REAL source row when the test row isn't visible; cleanup errors swallowed | tests | Running e2e can corrupt real triage state; suite can't gate anything | M | MED | `web/tests/e2e/global-setup.ts:70-113`, `cleanup.ts:9-21`, `pipeline-happy-path.spec.ts:49-64` (verified the real-row fallback) |
| SECURITY-06 | Timing-unsafe `===`/`!==` secret comparisons at the two most privileged gates, while `constantTimeEqual` exists in the same file (and is duplicated in http.ts) | security | Length/prefix timing leak; low practical exploitability, high drift-risk | S | LOW | `convex/agentToolsHttp.ts:61`, `convex/auth.ts:76-80` vs `auth.ts:26-40`, `http.ts:41-51` |
| SECURITY-05 | `CONVEX_DEPLOY_KEY` exposed job-wide in CI (incl. to `bun install` postinstalls) and never consumed by the job | security | Unnecessary supply-chain blast radius for an admin credential | S | LOW | `.github/workflows/public-editorial-export.yml:11-12` vs `scripts/export-editorial.ts:22-32` (reads only CONVEX_URL/DEV_BYPASS_SECRET) |
| SECURITY-09 | `seedCampaigns` test-seeding mutation deployed publicly (well-guarded, but guard = the bypass secret; seeds `visibility: "public"`) | security | Synthetic public campaigns sprayable by secret-holder | S | LOW | `convex/testing.ts:13-39` (verified requireAuth + assertBypassIdentity at :21-22) |
| DEBT-02 | 142-line hand-maintained string-based Convex API map in web (untyped `makeFunctionReference`), 17 commits of churn; generated typed API exists and is already used by scripts | debt | Backend renames compile clean in web, fail at runtime | M | MED | `web/src/integrations/convex/api.ts:1-142`; contrast `scripts/fetch-youtube-transcripts.ts:9` |
| CORRECTNESS-09 | `generate-experiment.ts` defaults to a stale Convex Cloud URL and never sends the bypass secret — documented flow cannot succeed | bug | Misleading failures for anyone following CLAUDE.md | S | LOW | `scripts/generate-experiment.ts:13,67-81` (verified both) |
| CORRECTNESS-10 | `smart-fetch.ts` without `--update`: `indexOf+1` resolves sourceId to `args[0]` (the URL) → mutation throws, swallowed by `.catch(console.error)`, fetched text never printed | bug | Tool's primary documented mode broken | S | LOW | `scripts/smart-fetch.ts:193-208` (verified) |
| DEPS-02 | Web build toolchain floats on `latest` of a 0.x package (`vite-plus`), incl. overrides; dead `vitest` override | deps | Any install can jump the toolchain to a breaking version; no CI to catch it (until plan 003) | S | LOW | `web/package.json:33-39` (verified) |
| DEPS-01 | Three dead root deps: `pdf-parse`, `pdf-parser-client-side` (two PDF libs, zero imports), `youtube-transcript` (transcripts come from Fabric CLI) | deps | Install weight + misleading manifest | S | LOW | grep: zero imports anywhere (verified) |
| DEPS-03 / DX-02 | `agent/` uses npm + package-lock.json in a Bun repo (its own scripts invoke bun/bunx); `.env.example` missing ~8 vars code reads (incl. `INGEST_SHARED_SECRET` — ingest denies ALL requests without it); no `web/.env.example` | dx | Wrong-tool installs; undocumented-env failures on ingest/e2e/Notion scripts | S | LOW | `agent/package-lock.json`, `agent/package.json:9-13`, `.env.example` (12 keys) vs `convex/http.ts:35`, README:56-60 |
| DOCS-01 | CLAUDE.md and AGENTS.md are near-duplicate 291-line files describing a repo without `web/` or `agent/`, no verification commands; they have ALREADY drifted (5 diff hunks incl. the secret vs placeholder) | docs | Primary agent instructions are wrong → agents don't know 2/3 of the codebase exists or that tests exist | S | LOW | diff verified; zero `web/`/`agent/` mentions in either |
| DOCS-02 | README lists 9 routes; `web/src/routes/` has 22 (missing: agent-runs, editorial, essays, failures, theses + details); verification list omits lint/e2e; n8n claim contradicts native cron polling | docs | A third of the app invisible from the entry-point doc | S | LOW | README:64-74 vs `ls web/src/routes` (22 files) |
| CORRECTNESS-11 | `weekOf` Monday off-by-a-week on Sundays + local/UTC mixing | bug | Folded into **plan 004** | — | — | `convex/weeklyBriefs.ts:300-303` |
| CORRECTNESS-04 | `maxTokens` invalid in AI SDK v6 — recipe generation uncapped | bug | Folded into **plan 003** (Step 4) | — | — | `convex/recipes.ts:481` |
| DEBT-03 | Five copy-paste Kernel.sh fetch scripts (three with hardcoded source-ID lists from past incidents); no archive convention among ~36 scripts | debt | Agents can't tell live tooling from spent one-offs | M | LOW | `scripts/fetch-*kernel*.ts`, `fetch-blocked-batch2.ts` (verified hardcoded IDs) |
| DEBT-05 | Roadmap-phase module names: `phase2.ts` is failure-domain types, `phase4.test.ts` tests editorial artifacts | debt | Greppability in an agent-executed repo | S | LOW | `convex/phase2.ts:1-12` (verified contents) |

## Direction findings (options for the maintainer — not ranked against bugs)

1. **Complete the agent-draft review loop (approve/reject/promote).** The schema models four statuses
   (`convex/schema.ts:180-195`) but only `pending_review` is ever written (`agentDrafts.ts:70`), and the
   detail UI renders disabled Approve/Reject/Rerun buttons (`agent-run-detail.tsx:294-300`). Closes the
   human-in-the-loop gap the whole agent surface builds toward, and stays consistent with the read-only
   external-surface decision (promotion is a human-initiated app mutation). Effort M (coarse). Strong evidence.
2. **Build `recipe_export_v1` (Scala .scl/.kbm from recipe parameters).** Items 6-7 of the maintainer's own
   build order (`docs/next-wave-roadmap.md:232-243`); zero export code exists (verified — `scales/*.scl` are
   hand-authored). Closes the "research-to-composition" loop; a deterministic pure generator is also ideal
   first test fodder. Effort M for .scl/.kbm, L for the full asset chain (coarse).
3. **Promote experiment protocol + revision diffs to first-class models.** Named in three strategy docs
   (`loop-spec-v1.1.md` §4a, `next-wave-roadmap.md` items 4-5, `meaning-roadmap.md:58-60`); currently
   `protocol` is an optional blob on recipes (`schema.ts:424`). Prerequisite for single-variable comparability.
   Effort L, MED risk (schema migration on live data) — design-spike plan recommended, not build-everything.
4. **Stand up an eval runner over the exported eval datasets.** `scripts/export-eval-datasets.ts` exports and
   `scripts/langsmith/upload-datasets.ts` uploads to LangSmith, but no scorer/runner exists in-repo, so prompt
   and model swaps (routine per docs) ship blind. "AI contract tests" is a named workstream
   (`next-wave-workstreams.md` §18). Effort M (coarse). MED confidence (runner design inferred).

## Findings considered and rejected / corrected during vetting

- **"CLAUDE.md and AGENTS.md are byte-identical"** (audit claim) — corrected: they differ in 5 hunks
  (secret vs placeholder, "Claude" vs "Codex", fence annotations). The substance (near-duplicates that drift)
  stands and is the DOCS-01 row above.
- **"Nothing consumes the eval datasets"** (audit claim) — corrected: `scripts/langsmith/upload-datasets.ts`
  uploads them; the missing piece is a runner/scorer (direction finding 4 reworded accordingly).
- **Swallowed LangSmith tracing errors** — not a finding: best-effort tracing is a recorded decision
  (decision log 2026-05-16). Excluded from the audit by instruction.
- **Read-only agent tool surface / snapshot-based public export / single active campaign / derived failure
  view** — by design per the decision log; not findings.
- **`agentRuns` public queries leaking data** — checked and rejected: `summarizeRun`/`summarizeEvent`
  (`convex/agentRuns.ts:77-103`) genuinely strip input/error/payload. The read-surface gap is the *content*
  modules (SECURITY-03), not agent runs.
- **Per-item `ctx.db.get` loops in Convex as N+1** — not flagged: document-by-id gets inside a query are
  normal Convex access patterns; only full-table collects and per-item *index scans* made the table.
